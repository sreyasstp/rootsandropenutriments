services:
  - type: web
    name: roots-and-rope
    env: static
    staticPublishPath: dist
    buildCommand: npm install && npm run build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
